---
title: "01-token-cost-optimization — Adaptive Roadmap"
date: "2026-06-20"
updated: "2026-06-20"
scope: "01-token-cost-optimization"
type: "plan"
status: "active"
version: "1.0"
agents:
  - "opencode"
tags:
  - "01-token-cost-optimization"
  - "plan"
  - "roadmap"
  - "kyro-ai"
changelog:
  - version: "1.0"
    date: "2026-06-20"
    changes: ["Roadmap created"]
related:
  - "[[README]]"
  - "[[RE-ENTRY-PROMPTS]]"
---

# 01-token-cost-optimization — Adaptive Roadmap

> Generated by `kyro-ai` on 2026-06-20

## Project Paths

| Path | Value |
|------|-------|
| Codebase | `/Users/rperaza/joicodev/projects/synapsync/kyro/kyro-ai` |
| Working Directory | `.agents/kyro/scopes/01-token-cost-optimization` |
| Findings | `.agents/kyro/scopes/01-token-cost-optimization/findings/` |
| Sprints | `.agents/kyro/scopes/01-token-cost-optimization/phases/` |

## Sprint Sizing Decision

```json
{
  "recommendedSprintCount": 3,
  "riskLevel": "medium",
  "rationale": "The remaining cost work adds a new public CLI surface, task-specific context selection, regression fixtures, and a separate model/budget routing contract. Splitting keeps the public command foundation, task-pack behavior, and budget-routing policy independently reviewable.",
  "splitTriggers": [
    "New public CLI command and options need isolated integration tests.",
    "Task-specific context packs depend on the base context-pack contract and need separate fixtures.",
    "Model/budget routing affects product policy and documentation independently from context-pack mechanics."
  ],
  "whyNotFewer": "Two sprints would mix CLI command plumbing, task-specific extraction, fixture budgets, and model routing policy into large review units with unclear rollback boundaries.",
  "whyNotMore": "Four or more sprints would add orchestration overhead without a new dependency boundary; docs and model routing can be completed together after context-pack fixtures exist.",
  "sprintProofs": [
    "Sprint 1 is independently provable when `kyro context-pack --kyro-scope <scope>` emits a bounded summary-first package for existing scopes with tests.",
    "Sprint 2 is independently provable when `--task <id>` context packs include only task-relevant details, rules, and validation criteria with budget regression fixtures.",
    "Sprint 3 is independently provable when budget/model routing is encoded in config/types/docs and validated without provider-specific model names."
  ]
}
```

Acceptance:

- `recommendedSprintCount` equals the sprint rows below.
- Each sprint has exactly one `sprintProofs` entry.
- Multi-sprint plans include at least one `splitTriggers` entry.
- `whyNotFewer` and `whyNotMore` are non-empty.

## Dependency Map

```text
Sprint 1: context-pack CLI foundation
  -> Sprint 2: task-specific context packs and budget fixtures
    -> Sprint 3: budget/model routing manifest and docs
```

## Sprint Summary

| Sprint | Finding Source | Version | Type | Focus | Dependencies | Proof | Status |
|--------|----------------|---------|------|-------|--------------|-------|--------|
| 1 | `findings/01-context-pack-command-foundation.md` | 3.4.x | feature | Base `kyro context-pack` command and scope/status output | — | Sprint 1 is independently provable when `kyro context-pack --kyro-scope <scope>` emits a bounded summary-first package for existing scopes with tests. | completed |
| 2 | `findings/02-task-specific-context-selection.md`, `findings/03-context-pack-token-regression-fixtures.md` | 3.4.x | feature | Task-specific packs, matching rules, and budget fixtures | Sprint 1 | Sprint 2 is independently provable when `--task <id>` context packs include only task-relevant details, rules, and validation criteria with budget regression fixtures. | completed |
| 3 | `findings/04-model-budget-routing-manifest.md` | 3.4.3 | feature | Provider-neutral budget/model routing manifest and docs | Sprint 2 | Sprint 3 is independently provable when budget/model routing is encoded in config/types/docs and validated without provider-specific model names. | completed |

## Sprint Definitions

### Sprint 1 — Context-pack CLI Foundation

- **Source**: `findings/01-context-pack-command-foundation.md`
- **Version Target**: 3.4.x
- **Type**: feature
- **Focus**: Add `kyro context-pack --kyro-scope <scope>` with summary-first output and JSON mode.
- **Dependencies**: None
- **Proof**: Sprint 1 is independently provable when `kyro context-pack --kyro-scope <scope>` emits a bounded summary-first package for existing scopes with tests.
- **Suggested Phases**:
  1. CLI contract — add command routing, option parsing, and scope artifact reads.
  2. Output contract — emit text/JSON context packs with warnings and estimated token totals.
  3. Validation — add fixture/golden tests and docs for the new command.

### Sprint 2 — Task-specific Packs and Budget Fixtures

- **Source**: `findings/02-task-specific-context-selection.md`, `findings/03-context-pack-token-regression-fixtures.md`
- **Version Target**: 3.4.x
- **Type**: feature
- **Focus**: Add `--task <id>` packs, relevant rule selection, and context-pack budget regression tests.
- **Dependencies**: Sprint 1
- **Proof**: Sprint 2 is independently provable when `--task <id>` context packs include only task-relevant details, rules, and validation criteria with budget regression fixtures.
- **Suggested Phases**:
  1. Task selection — resolve active task details from summaries/index and expose fallback Markdown paths.
  2. Rule and evidence filtering — include only matching `rules.index.json` entries and compact evidence references.
  3. Regression fixtures — add context-pack fixture checker and budget thresholds.

### Sprint 3 — Budget Routing Manifest

- **Source**: `findings/04-model-budget-routing-manifest.md`
- **Version Target**: 3.4.x
- **Type**: feature
- **Focus**: Encode provider-neutral budget classes and expose them through context-pack output/docs.
- **Dependencies**: Sprint 2
- **Proof**: Sprint 3 is independently provable when budget/model routing is encoded in config/types/docs and validated without provider-specific model names.
- **Suggested Phases**:
  1. Manifest contract — define budget classes in config/types without provider-specific model names.
  2. Context integration — include selected budget class in context-pack output and validation fixtures.
  3. Documentation — update cost/context docs with budget-class tradeoffs and quality boundaries.
